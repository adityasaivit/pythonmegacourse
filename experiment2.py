todos=[]
while(True):
    user_prompt = "Enter a ToDo:"
    todo=input(user_prompt)
    todos.append(todo.title())
    print(todos)


#above code runs for infinite time

# title will change all the first letters of a word in a sentence

